{
  "name": "wait-for",
  "version": "1.0.0",
  "description": "waits for a condition to be met before proceeding",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.0.4"
  }
}
